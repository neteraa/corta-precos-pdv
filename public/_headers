# HTML pages: never cache — so browser always fetches fresh HTML
# (which has up-to-date fingerprinted asset URLs)
/*
  Cache-Control: no-cache, no-store, must-revalidate

# Hashed assets: cache forever — safe because filename changes with content
/assets/*
  Cache-Control: public, max-age=31536000, immutable
